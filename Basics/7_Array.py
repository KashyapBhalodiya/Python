# Arrays:
# python lists are using for arrays.
# Working with arrays we should import a library like the Numpy library

# Arrays are used to store multiple values in one variable.
cars = ["Ford", "Volvo", "BMW"]
print(cars)

# Access the element
print(cars[0])
print(len(cars)) # Length of cars

# Methods
"""
append, clear, copy, count, extend, index, insert, pop, remove, reverse, sort
"""